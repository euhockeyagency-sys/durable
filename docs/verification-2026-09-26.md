# Проверка опубликованных изменений — 26 сентября 2026

Проверяемая версия репозитория: `1f49d8f`. Изменения приложения при этой проверке не публиковались.

## Подтверждено

- [Деплой версии 1f49d8f](https://github.com/euhockeyagency-sys/durable/actions/runs/36182992687) завершился успешно.
- В Supabase зарегистрирована миграция `20260925220000_application_followups`; таблица `public.applications` содержит `internal_note` (`text`) и `next_contact_at` (`date`).
- Опубликованная функция `cleanup-applications` активна и исключает из очистки заявки с датой следующего контакта сегодня или позже. Проверен код функции; удаление данных не запускалось.
- В базе есть одна заявка со структурированным результатом калькулятора и одна запись с непустой внутренней заметкой. Это агрегаты на момент проверки, без выгрузки персональных данных.
- Во время проверки 25 сентября журнал уведомлений за предшествующие 24 часа содержал две записи `sent` для email и две для Telegram. Это подтверждает статус отправки в приложении, но не прочтение сообщения получателем.

## Браузерная проверка аналитики

Проверены английская и русская версии при ширине окна 390 px. Публичные HTML, CSS и JavaScript загружены с production через curl и исполнены в отдельном Chrome через Playwright. Внешние запросы аналитики и запросы записи заблокированы: проверка не создаёт заявки, уведомления или тестовые конверсии в аналитических системах.

| Сценарий | Результат в обеих локалях |
| --- | --- |
| Первое взаимодействие с калькулятором | Создаётся `calculator_start` |
| Расчёт уровня | Создаётся `calculator_done` с `band` и `score` |
| Переход из результата в форму | Заполняются год рождения, позиция и контекст калькулятора |
| Первое взаимодействие с формой | Создаётся `form_start` с `from_calculator: true` |
| Отображение страницы успеха с синтетическим корректным номером | Создаётся `application_sent` с `from_calculator: true` |
| Перезагрузка этой страницы успеха в той же сессии | Повторное событие не создаётся |
| Страница успеха с некорректным номером | Событие не создаётся |
| Ошибки JavaScript в проверенных сценариях | Нет |

События проверялись в `dataLayer`. Это подтверждает работу браузерного кода, но не доставку и отображение событий в отчётах GA4/Метрики. Предыдущий пустой результат проверки через Firecrawl не воспроизвёлся в отдельном Chrome и сам по себе не доказывает неисправность сайта.

## Кабинет

На Node.js 22 успешно прошли девять существующих тестов кабинета и контекста калькулятора:

```bash
node --test --test-name-pattern='admin|calculator context' test/app.test.js
```

Проверены вход и ограничения сессии, защита CSRF, статусы, фильтры, сохранение заметки и даты следующего контакта, валидация и экранирование. Тесты используют моки хранилища и уведомлений.

## Что ещё требовало проверки — итог на 26 сентября 2026

1. **Поступление событий в GA4/Метрику.**
   - GA4: подтверждено 25 сентября на production с принятыми cookies. Сайт отправил в `region1.google-analytics.com/g/collect` (ответ 204) `calculator_start` и `calculator_done` с `band=top`, `score=90`. События `form_start` и `application_sent` в GA4 реальным трафиком не проверялись.
   - Метрика: запросы к `mc.yandex.com` в среде проверки остаются `pending`, доставку целей подтвердить нельзя. Остаётся сверить отчёт «Конверсии» вручную.
   - Не сделано (требует доступа к кабинетам): в Метрике (счётчик 110889446) завести цели «JavaScript-событие» `calculator_start`, `calculator_done`, `form_start`, `application_sent`; в GA4 отметить `form_start` и `application_sent` ключевыми событиями.
2. **Сохранение даты следующего контакта через production-кабинет.** Сделано 25 сентября: сохранение `internal_note` и `next_contact_at` (`2099-01-01`) на реальной записи вернуло 303, значения прочитались из страницы и нашлись фильтром `upcoming`, затем поля очищены (запись возвращена в `null`). Отказы: без CSRF-токена 403, невозможная дата 400. Сейчас в базе записей с датой нет, это ожидаемо.
3. **Полная отправка новой заявки через production.** Сделано владельцем в согласованном контрольном сценарии: заявка `EHA-202609-698818` (EN, из калькулятора). Подтверждено: запись в базе с `source.calculator` (`top`, 87, три лиги), аудит уведомлений `email: sent` и `telegram: sent`, отображение в кабинете. Заявка помечена «ТЕСТ» и переведена в `archived`. Текст самих сообщений в Telegram и почте не проверялся.
4. **Русская заявка** из `/ru/kalkulyator-urovnya` не отправлялась, в базе её нет. Пока проверена только английская.

## Состояние на 26 сентября

- Production на `1f49d8f`, службы `eha` и `eha-mcp` активны, серверных коммитов вне GitHub нет.
- В базе 38 заявок: 1 с результатом калькулятора, 1 с внутренней заметкой, 1 в `archived`. За последние 48 часов уведомлений отправлено 4, неудачных 0.
- Миграции и функции из `supabase/` выкладывает интеграция Supabase с GitHub (чек «Supabase Preview»), а не `deploy.yml`: шаг миграций в workflow пропускается, пока в секретах нет Supabase-токенов.
